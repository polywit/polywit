#!/usr/bin/env python3
import os
import sys

sys.dont_write_bytecode = True  # prevent creation of .pyc files
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "../polywit"))

import polywit.__main__

if __name__ == "__main__":
    sys.exit(polywit.__main__.main())
